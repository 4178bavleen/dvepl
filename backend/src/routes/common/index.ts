// const kafkaTestingRoutes = require("./kafkaTesting/index");

async function commonRoutes(fastify, options) {
  // Public routes
  // fastify.register(kafkaTestingRoutes, { prefix: "/kafkaTesting" });
}

module.exports = commonRoutes;
