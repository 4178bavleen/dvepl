# TODO: Attach Excel sheet to vendor follow-up email

## Steps
- [ ] Update `handleSendFollowUp` in `dashboard/src/pages/inventory/vendorTracking.tsx` to generate the Excel report as base64 and send it as an attachment with the email
- [ ] Verify the email sends with the Excel attachment
