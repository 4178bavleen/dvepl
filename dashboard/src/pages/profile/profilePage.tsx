import React from 'react';
import { SettingsPage } from '@/pages/settings/settingsPage';

export function ProfilePage() {
  // We can render the main settings view directly
  return <SettingsPage />;
}

export default ProfilePage;
